import 'package:flutter/material.dart';

class Videoscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Videos (102)')),
      body: Center(child: Text('All your videos would appear here')),
    );
  }
}