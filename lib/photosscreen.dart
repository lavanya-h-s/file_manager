import 'package:flutter/material.dart';

class PhotosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Photos (3362)')),
      body: Center(child: Text('All your photos would appear here')),
    );
  }
}